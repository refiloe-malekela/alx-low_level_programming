#include "main.h"

/**
 * _isdigit ->  a function that checks for a digit (0 through 9).
 *
 * @c : integer paramer
 *
 * Return: something but success
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
